#include<stdio.h>
// the header above tells the compiler to include certain library or a file

// main function is the body of the program, contains all the logic and functionality
void main(void) {
    // printf is a function that prints to standart output 
    printf("Hello World!"); // in C, semicolon is not optional
    return;
}